{% extends 'base.html.twig' %}

{% block title %}New Genre{% endblock %}

{% block body %}
    <h1>Create new Genre</h1>

    {{ include('genre/_form.html.twig') }}

    <div class="page-actions">
        <a class="btn btn-secondary" href="{{ path('app_genre_index') }}">Retourner à la liste</a>
    </div>
{% endblock %}
